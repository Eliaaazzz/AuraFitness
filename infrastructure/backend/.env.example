# Docker Compose Environment Variables
# Copy this file to .env and customize for your environment

# ==============================================================================
# DOCKER COMPOSE SETUP (Local Development & Testing)
# ==============================================================================
# The docker-compose.yml uses local PostgreSQL and Redis containers.
# Only customize the values below if you need different settings.

# Spring Profile (dev or prod)
SPRING_PROFILES_ACTIVE=dev

# Database Configuration (used by both app and postgres container)
POSTGRES_PASSWORD=dev_password

# Redis Configuration (optional password)
REDIS_PASSWORD=

# API Keys (Optional - app works without these)
OPENAI_ENABLED=false
OPENAI_API_KEY=
YOUTUBE_API_KEY=
SPOONACULAR_API_KEY=

# Application Settings
APP_SEED_ENABLED=true

# JVM Options (optional override)
# JAVA_OPTS=-Xms256m -Xmx512m -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0

# ==============================================================================
# AWS DEPLOYMENT (EC2/ECS with external RDS & ElastiCache)
# ==============================================================================
# For production deployment, use these instead:
# DB_HOST=your-rds-endpoint.rds.amazonaws.com
# DB_PORT=5432
# DB_NAME=fitness_db
# DB_USERNAME=admin
# DB_PASSWORD=YourSecurePassword123!
# REDIS_HOST=your-redis-endpoint.cache.amazonaws.com
# REDIS_PORT=6379
